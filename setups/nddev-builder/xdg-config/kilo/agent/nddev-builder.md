---
description: NDDev Kilo setup-module builder and reviewer.
mode: all
---

Use `instructions/nddev-builder.md` and the `nddev-builder` Skill toolkit.
When changing a setup module, keep public implementation and public docs in the
module, keep private validation in the private harness, preserve unmanaged
target state, and fail closed on unsupported native Kilo surfaces.
